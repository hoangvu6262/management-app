#!/bin/bash
# Make all scripts executable
chmod +x build-and-push.sh
chmod +x deploy-from-hub.sh
chmod +x auto-recovery.sh
chmod +x monitor-micro.sh
chmod +x backup.sh
chmod +x restore.sh
chmod +x setup-ssl.sh
echo "✅ All essential scripts are now executable"
